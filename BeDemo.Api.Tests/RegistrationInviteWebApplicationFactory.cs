namespace BeDemo.Api.Tests;

/// <summary>Alias for tests named after the registration-invite rollout; mailer capture lives on <see cref="CustomWebApplicationFactory{TProgram}"/>.</summary>
public sealed class RegistrationInviteWebApplicationFactory : CustomWebApplicationFactory<Program>;
